name: Update PR Templates
on:
  workflow_dispatch:
    inputs:
      repo-type:
        required: true
        type: choice
        description: Repo Type
        options:
          - terraform
          - chef
      
jobs:
  pr-template-update:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - name: Update PR Templates
        run: | 
          git submodule add https://github.com/tahsib-optimizely/pr-templates.git pr-templates
          chmod +x ./pr-template/sync.sh
          bash ./pr-template/sync.sh ${{ github.event.inputs.env }}

      - name: Clean Up Submodule
        run: |
          chmod +x ./pr-template/cleanup.sh
          bash ./pr-template/cleanup.sh
        
      - name: Push to new-pr-template branch
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: pr-templates-updated
          branch: new-pr-templates
          create_branch: true
          push_options: --force